angular.module('buildBikes')
       .controller('drawingController' , drawingController);

      function drawingController($http) {

        this.saveImage = function(){

            // var image = new Image();
            // image.id = "pic"
            // image.src = document.getElementById('pwCanvasMain').toDataURL();
            
            // document.body.appendChild(image);

        }

      }
